# Landing explorations

Open `/prototypes/landing?v=2` for the map-first design. The map design is promoted to `/`; this route retains the three design explorations. Shared production components live in `components/landing`, and the interview is at `/assessment`.

The featured people link to their saved live-journey results. The horizontal coordinate is expressed outlook; the vertical coordinate is expected scale of transformation. Both come from the latest recorded suite. The other two design directions retain their original three examples.

Portrait labels are separated when they collide, with connector lines and dots retaining their actual measured positions. Portraits and the legend both link to results. Current X image provenance is in `public/personas/SOURCES.md`.
