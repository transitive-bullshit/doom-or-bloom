# Bookmark coverage and source review — September 25, 2026

The 97-user source expansion introduced 266 distinct non-tweet source URLs without cache entries. This pass preprocesses those links and adds durable local artwork fallbacks for all source bookmarks. Existing immutable assessments are not regenerated by metadata or source changes.

## Preview handling

The initial publisher fetch supplied images for 227 of the 266 new URLs. Text-only pages such as Gwern's essays, Eli Lifland's AXRP interview and Julia Galef's open questions lacked publisher artwork. Rob Miles's Inside View transcript instead embeds a YouTube episode, so the publisher's oEmbed thumbnail supplies an episode-specific image. Some sources reject automated retrieval; others supply no usable image or favicon.

The script now resolves direct/embedded YouTube videos, tries multiple publisher icons, accepts reviewed browser captures, and renders a local title/domain card when no image is obtainable. Missing favicons receive a local site-initial mark. Both generated fallback types are recorded in the cache and remain eligible for replacement on a later refresh. No paid image generation was needed. All images are processed WebP assets served locally; opening a profile does not trigger publisher thumbnail fetches.

Source summaries now reach bookmarks directly instead of being discarded by the server loader. The regression suite checks every current authored non-tweet source for a local image, icon and description.

## Discovery is not opinion evidence

Removed all 12 `independent.prose.md` entries from the current source briefs, removed its cached bookmark entry, and excluded it from future preview inventory. The directory remains in historical intake/research provenance. Frozen simulation snapshots retain their original provenance; profile source lists use the cleaned current briefs.

## Rob Miles's saved placement

The original simulation uses two brief source summaries and three generated answers. Its second answer explicitly adopts a conditional rather than optimistic/pessimistic expectation, and its saved Doom–Bloom coordinate is 0.4925. Its approximate 10% P(doom) is an inferred display estimate with a broad 0–59% range, not a public Miles quotation. The source summaries omitted important interview material. Expanding and correcting the brief is necessary before a meaningful new simulation; a desired map position is not a scoring target.

## Research integration

[Robert Miles review](robert-miles-source-review-2026-09-25.md): seven → eleven sources, including three foundational videos and a substantial 2025 interview, with the omitted 2022 pessimistic outlook restored. [The broader audit](independent-third-party-evidence-audit-2026-09-25.md) adds four Latent Space sources to swyx (six → ten) and one verified interview apiece to Hanson, Zvi and Mollick (six → seven each). Biderman’s newly discovered episode remains a transcript/audio follow-up; show notes were not treated as verified personal statements. This is a structural audit of all 97 briefs plus targeted research, not a second exhaustive search for every person.

The final catalog has 707 source records: 708 prior records, minus 12 discovery-list entries, plus 11 substantive additions. There are 276 distinct current non-tweet links among the 97 additions, all with cached local images, icons/site marks and descriptions. The complete current source/resource inventory has 559 non-tweet URLs with images and zero preview gaps. The cache also retains previews used by older frozen records.

## Validation

- `pnpm test` passed: 288 tests plus formatting, lint, types, content validation and Knip.
- Both bookmark browser cases passed, including local images, source descriptions, mobile text overflow and the removed directory link.
- `pnpm build:local` passed with production trace checks.
- Visual review of source-page contact sheets excluded blank/challenge/overlay captures; final Rob Miles bookmarks were inspected at 1200×816.
- Local profile synchronization updated 97 briefs, while preserving all 141 selected results and all 44 featured flags.
- `git diff --check` passed. The generated journey fixture is unchanged. No inference regeneration or deployment was performed.
