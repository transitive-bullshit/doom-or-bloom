# README screenshots

Captured from the local application on September 22, 2026, in light mode at 2× pixel density. No new inference calls were made.

- `featured-map.jpg`: the homepage’s `.map-study` section, at a 1440px viewport.
- `jev-state.png` and `jev-result.png`: Jensen Huang’s recorded simulated assessment, reached through his homepage portrait. Open **Debug info**, fold the top-level JSON fields, and capture **Final assessment state** / **Final generated result** at a 700px viewport. The Next.js development overlay is hidden for capture.

The state is the recorded projection input. The result is the application’s composed result, not a raw Jev response. These contain only public simulated-persona data.
